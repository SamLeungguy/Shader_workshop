using UnityEngine;

public class Dissolve : MonoBehaviour
{
    public Material mat;

    // Update is called once per frame
    void Update()
    {
        if (mat)
        {
            //mat.SetFloat("test", Time.time);
        }
    }
}
